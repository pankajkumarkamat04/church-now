# Backend API base (no trailing slash). Required for local Next + Express on different ports.
NEXT_PUBLIC_API_URL=http://localhost:5000

# Production: set to your public API origin, e.g. https://api.your-domain.com
# Or leave empty only if nginx proxies /api to the backend on the same host.
